{
    "name": "aws-overview-dice-api",
    "version": "1.0.0",
    "description": "AWS Overview Workshop - Dice API demo leveraging multiple AWS services",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "node build.mjs",
        "test": "vitest run",
        "test:watch": "vitest",
        "typecheck": "tsc --noEmit",
        "lint": "eslint src/ tests/"
    },
    "dependencies": {
        "@aws-lambda-powertools/logger": "^2.0.0",
        "@aws-sdk/client-dynamodb": "^3.0.0",
        "@aws-sdk/client-secrets-manager": "^3.0.0",
        "@aws-sdk/client-sns": "^3.0.0",
        "@aws-sdk/lib-dynamodb": "^3.0.0",
        "@aws-sdk/util-dynamodb": "^3.996.2",
        "zod": "^3.23.0"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.0",
        "@types/node": "^22.0.0",
        "esbuild": "^0.25.0",
        "eslint": "^9.0.0",
        "typescript": "^5.7.0",
        "vitest": "^3.0.0"
    }
}
